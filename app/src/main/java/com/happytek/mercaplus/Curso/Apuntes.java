package com.happytek.mercaplus.Curso;

public class Apuntes {
/*
    var miVariable = "Hola"

    var miNumero : Int = 1

    var miLong : Long = 100000000

    var miFloat : Float = 1.23F

    var miDouble : Double = 3.2323

    **********************WHEN*****************

    when (a) {
        1 -> Log.d(1)
        2 -> Log.d(2)
        3,4,5,6 -> Log.d(3,4,5,6)
        in 6..100 -> Log.d(del 6 al 100)
        !in 6..100 -> Log.d(No entre 6 y 100)
        else -> {
                Log.d(otro)
            }
        }

        *****************FOR*****************

        for (numero in 35..40) {
            Log.d(El valor del numero es $numero)
        }

        for (canalTV in canalesTV {
            Log.d(canalTV)
        }

        ***************WHILE************
        var contador :Int = 1
        while (contador <=10){
            Log.d(El valor de contador es $contador)
            contador++
            }


        ************DO WHILE***************

            contador = 1
            do {
                contador++;
            } while (contador <= 10) {

               Log.d(El valor de contador es $contador)
            contador++

            }


            **************CONTINUE^**********

            Como break, pero solo hace que no se ejecute la siguiente línea de codigo.

            var contador :Int = 1
            while (contador <=10){
            if(contador == 5) {
                continue
                }
            Log.d(El valor de contador es $contador) -->Cuando tiene el valor 5 no lo pinta.
            contador++
            }


            *********ARRAYS*******************

            var canalesTv = arrayOf("La 1", "La 2", "Antena 3")

            var canal = canalesTv.get(0)
            Log.d("El canal es ${canalesTV.get(0)}")
            canalesTv.set(1, "La primera")
            canalesTv.size


            var arrayCanalesTv = ArrayList<String>()
            arrayCanalesTv.add("La uno")

            if (arrayCanalesTv.contains("La uno")) -> true

            arrayCanalesTv.remove("La uno")


            ***************HASHMAP*************

            var hashMapCanales = HashMap<Int, String>
            hashMapCanales.put(1, "La uno")
            hashMapCanales.get(1)
            hashMapCanales.put(1, "UNO") -> actualiza

            for (claves in hashMapCanalesTV.keys) {
            Log.d(hasMapCanalesTv.get(claves)
            }


            *********FUNCIONES***********
            fun alCuadrado(numero: Int) :Int {


                return numero*numero
            }

            fun mostrarAlgo(mostrar:String):Unit {  --void

            }

             fun mostrarAlgo(mostrar:String){  --void

            }

 */

}
